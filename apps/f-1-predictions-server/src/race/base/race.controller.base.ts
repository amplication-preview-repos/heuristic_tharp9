/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { RaceService } from "../race.service";
import { RaceCreateInput } from "./RaceCreateInput";
import { Race } from "./Race";
import { RaceFindManyArgs } from "./RaceFindManyArgs";
import { RaceWhereUniqueInput } from "./RaceWhereUniqueInput";
import { RaceUpdateInput } from "./RaceUpdateInput";
import { BetFindManyArgs } from "../../bet/base/BetFindManyArgs";
import { Bet } from "../../bet/base/Bet";
import { BetWhereUniqueInput } from "../../bet/base/BetWhereUniqueInput";

export class RaceControllerBase {
  constructor(protected readonly service: RaceService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Race })
  async createRace(@common.Body() data: RaceCreateInput): Promise<Race> {
    return await this.service.createRace({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        date: true,
        location: true,
        results: true,
        schedule: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Race] })
  @ApiNestedQuery(RaceFindManyArgs)
  async races(@common.Req() request: Request): Promise<Race[]> {
    const args = plainToClass(RaceFindManyArgs, request.query);
    return this.service.races({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        date: true,
        location: true,
        results: true,
        schedule: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Race })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async race(
    @common.Param() params: RaceWhereUniqueInput
  ): Promise<Race | null> {
    const result = await this.service.race({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        date: true,
        location: true,
        results: true,
        schedule: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Race })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateRace(
    @common.Param() params: RaceWhereUniqueInput,
    @common.Body() data: RaceUpdateInput
  ): Promise<Race | null> {
    try {
      return await this.service.updateRace({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          date: true,
          location: true,
          results: true,
          schedule: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Race })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteRace(
    @common.Param() params: RaceWhereUniqueInput
  ): Promise<Race | null> {
    try {
      return await this.service.deleteRace({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          date: true,
          location: true,
          results: true,
          schedule: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/bets")
  @ApiNestedQuery(BetFindManyArgs)
  async findBets(
    @common.Req() request: Request,
    @common.Param() params: RaceWhereUniqueInput
  ): Promise<Bet[]> {
    const query = plainToClass(BetFindManyArgs, request.query);
    const results = await this.service.findBets(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        amount: true,
        prediction: true,

        user: {
          select: {
            id: true,
          },
        },

        race: {
          select: {
            id: true,
          },
        },

        userReference: true,
        raceReference: true,
        predictionDetails: true,
        userRelation: true,
        raceRelation: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/bets")
  async connectBets(
    @common.Param() params: RaceWhereUniqueInput,
    @common.Body() body: BetWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      bets: {
        connect: body,
      },
    };
    await this.service.updateRace({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/bets")
  async updateBets(
    @common.Param() params: RaceWhereUniqueInput,
    @common.Body() body: BetWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      bets: {
        set: body,
      },
    };
    await this.service.updateRace({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/bets")
  async disconnectBets(
    @common.Param() params: RaceWhereUniqueInput,
    @common.Body() body: BetWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      bets: {
        disconnect: body,
      },
    };
    await this.service.updateRace({
      where: params,
      data,
      select: { id: true },
    });
  }
}
